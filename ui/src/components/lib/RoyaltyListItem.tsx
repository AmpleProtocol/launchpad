import { IContent } from "@ample-launchpad/client"
import { Box } from "theme-ui"

interface IRoyaltiListItemProps {
	content: IContent,
	onClick: () => any
}
export const RoyaltyListItem: React.FC<IRoyaltiListItemProps> = ({ content, onClick }) => {
	return <Box></Box>
}
